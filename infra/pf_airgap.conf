# pf_airgap.conf
# This configuration isolates the machine by blocking outbound traffic on main interfaces
# while allowing local loopback for GAURDA services.

# Allow all traffic on loopback
set skip on lo0

# Block outbound on en0 and en1 (common macOS Wi-Fi/Ethernet)
block drop out on en0 all
block drop out on en1 all
