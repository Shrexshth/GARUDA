global:
  ollama_base_url: "http://localhost:11434"

routes:
  vision_task: "llava:7b" # Qwen2.5-VL isn't natively on standard Ollama library yet, so fallback to llava for vision stability
  reasoning_task: "qwen2.5:7b"
  embeddings: "nomic-embed-text"
